import React from "react";
import Map from "../Components/MapComponent";
import MapView from "react-native-maps";

const Home = () => {
  return (
    <MapView>
      <Map />
    </MapView>
  );
};

export default Home;
